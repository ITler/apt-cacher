# Docker based apt-cacher construction kit
 * uses `apt-cacher-ng`
 * provides upstart scripts for
    1. starting container during boot and 
    2. setting host's apt-proxy by using ip address of container
